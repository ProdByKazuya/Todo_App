import styled from "styled-components";

export const SParagraph = {
    P: styled.p`
      font-size: 16px;
      margin: 10px 0;
      color: #333;
      font-weight: bold;
      display: inline-flex;
      justify-content: center;
    `
}